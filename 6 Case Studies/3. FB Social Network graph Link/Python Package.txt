Python Package

-> Pre-Featuriezed Data is stored in HDF file format (Hierarchical Data Format)

networkx   //  for Graph & Network DS
  \
   .read_edgelist()
   .info()          // provide stats abt Graph
   .draw()          // draw the graph

   .DiGraph()   // DirectedGraph 

   .pagerank()              // compute the page rank for Directed graph
   .shortest_path_length()  // give the shortes path lenngth between 2 nodes in Graph
   .katz.katz_centrality()
   .hits()                  // HITS algorithm

   graph_obj   // of networkx
      \
       .nodes()


HDFStore()  // to store data in hdf file fomat (Hierarchical Data Format)
